# V1 验证记录

验证日期：2026-09-11。

## 已通过

- `npm install` 成功；更新 Vitest 至 4.1.11 后 npm 报告 0 个已知依赖漏洞。
- `npm run typecheck`：TypeScript 严格检查通过，无显式 `any`。
- `npm test`：15 项测试通过，覆盖题库准入、无重复抽题、类别配额与补位、四种题型、超时、时间奖励、连胜与暴走、结果汇总、称号边界、历史排序与损坏存储恢复。
- `npm run build`：生成 `dist/`，资源路径包含 `/doghouse-lol-cup/`。
- 浏览器：正式首页、空题库报名禁用、规则页、历史空状态均可访问；未发现 JavaScript 错误。
- 本地独立软件夹具：完成单选、多选、判断、情景控件，验证正确反馈、错误反馈、超时、答案锁定、解析展开、第 3 题连胜 +50、答错 Combo 清零与结果页。
- 已看到结果页的总分、正确率、最长 Combo、平均耗时、分类明细与最高难度错题；测试夹具故意不写入正式历史，亦验证了未保存提示的显示。
- 手机视口 390×844：答案按钮实测 58px，题干 19px，无横向溢出。
- 320px 窄屏结果页、412px 手机答题页：DOM 尺寸检查无横向溢出。
- 桌面视口 1440×1000：首页赛事布局显示正常，无横向溢出。
- 正式 `questions.json` 只有禁用且未审核的占位模板，0 道正式可用题；没有生成 LOL 事实或答案。
- 软件夹具仅经 Vite 开发入口使用，正式构建不发布 `tests/ui.html`。

## 验证范围

响应式测试使用浏览器视口模拟，没有真机 Safari / Android 实机测试。localStorage 清空与排序已经过自动化测试；没有清除用户已有浏览器数据。未提供正式题库，因此没有验证任何 LOL 题目事实。

GitHub Actions 与 Pages 配置已经编写；在线发布必须在成功创建仓库后验证，不能用本地构建成功代替线上部署成功。
